@model Restaurante.Models.Permiso

@{
    ViewData["Title"] = "Editar permiso";
}
<style>
    body {
        font-family: 'Playfair Display', serif;
        font-style: italic;
    }

    .centered-text {
        text-align: center;
        font-weight: bold;
        color: #333;
    }

    .bold-text {
        font-weight: bold;
    }

    .date-input {
        width: 150px;
        display: inline-block;
    }
</style>
@if (User.Identity!.IsAuthenticated) // sirve para bloquear ver los elementos si no estas registrado
{
<br />
<h3 class="bold-text">~ Permisos ~</h3>
<hr />
<br />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Numero" />
            <div class="form-group">
                <label asp-for="Descripcion" class="control-label"></label>
                <input asp-for="Descripcion" class="form-control" />
                <span asp-validation-for="Descripcion" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Roles" class="control-label"></label>
                <select asp-for="Roles" class="form-control" asp-items="ViewBag.IdRol"></select>
                <span asp-validation-for="Roles" class="text-danger"></span>
                </div>
                <br />
            <div class="form-group">
                    <input type="submit" value="Guardar" class="custom-btn" />
            </div>
        </form>
    </div>
</div>
    <br />
    <div>
        <a class="custom-btn" href="@Url.Action("Index", "Permiso")">Volver</a>
    </div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
}